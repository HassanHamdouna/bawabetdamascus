import 'package:flutter/material.dart';

class AppColors {
  // 🎯 الألوان الأساسية
  static const Color primary = Color(0xFFD4AF37); // ذهبي فاخر
  static const Color secondary = Color(0xFFB58900);
  static const Color accent = Color(0xFFE0C066);

  // ⚪ الخلفيات
  static const Color backgroundLight = Color(0xFFF9F9F9);
  static const Color backgroundDark = Color(0xFF121212);

  // 🧱 البطاقات والعناصر
  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color darkCard = Color(0xFF1F1F1F);
  static const Color darkContainer = Color(0xFF2C2C2C);

  // 🖤 النصوص
  static const Color textLight = Colors.black87;
  static const Color textDark = Colors.white;

  // 🧭 التفاعل
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);

  // 🧊 محايدة
  static const Color grey = Colors.grey;
  static const Color divider = Color(0xFFE0E0E0);

  static const Color transparent = Colors.transparent;
}
